const act = true;

//let msg = "";

//if (act) {
//  msg = "Activo"
//} else {
//  msg = "Inactivo"
//};

//const msg = (act) ? "Activo": "Inactivo";   // codigo ternario
const msg = act && "Activo";  // solo si una condicion se cumple. Es solo hacer un if (&&).

console.log(msg);
